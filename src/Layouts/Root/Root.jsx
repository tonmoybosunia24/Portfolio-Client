import { Outlet } from "react-router";

const Root = () => {
       return (
              <div className="font-Rajdhani">
                     <Outlet></Outlet>
              </div>
       );
};

export default Root;